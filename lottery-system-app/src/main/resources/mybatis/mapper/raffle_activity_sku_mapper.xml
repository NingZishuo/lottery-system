<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lottery.infrastructure.persistent.dao.IRaffleActivitySkuDao">

    <resultMap id="dataMap" type="com.lottery.infrastructure.persistent.po.RaffleActivitySku">
        <id column="id" property="id"/>
        <result column="sku" property="sku"/>
        <result column="activity_id" property="activityId"/>
        <result column="activity_count_id" property="activityCountId"/>
        <result column="stock_count" property="stockCount"/>
        <result column="stock_count_surplus" property="stockCountSurplus"/>
        <result column="product_price" property="productPrice"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryRaffleActivitySku" resultMap="dataMap">
        SELECT *
        FROM raffle_activity_sku
        <where>
            <if test="sku != null">
                sku = #{sku}
            </if>
            <if test="activityId != null">
                and activity_id = #{activityId}
            </if>
        </where>
    </select>

    <select id="queryRaffleActivitySkuList" resultMap="dataMap">
        SELECT *
        FROM raffle_activity_sku
        where activity_id = #{activityId}
    </select>

    <!-- void updateActivitySkuStock(Long sku);-->
    <update id="updateActivitySkuStock">
        update raffle_activity_sku
        set stock_count_surplus = stock_count_surplus-1 ,update_time = now()
        <where>
            <if test="sku != null">
                sku = #{sku}
            </if>
        </where>
        and stock_count_surplus > 0
    </update>

    <!--void clearActivitySkuStock(Long sku);-->
    <update id="clearActivitySkuStock">
        update raffle_activity_sku
        set stock_count_surplus = 0,update_time = now()
        <where>
            <if test="sku != null">
                sku = #{sku}
            </if>
        </where>
        and stock_count_surplus > 0
    </update>


</mapper>
