<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lottery.infrastructure.persistent.dao.IRaffleActivityAccountDao">

    <resultMap id="dataMap" type="com.lottery.infrastructure.persistent.po.RaffleActivityAccount">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="activity_id" property="activityId"/>
        <result column="total_count" property="totalCount"/>
        <result column="total_count_surplus" property="totalCountSurplus"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.lottery.infrastructure.persistent.po.RaffleActivityAccount">
        insert into raffle_activity_account(user_id, activity_id, total_count, total_count_surplus)
        values (#{userId}, #{activityId}, #{totalCount}, #{totalCountSurplus})
    </insert>

    <!--int addAccountQuota(RaffleActivityAccount raffleActivityAccount);-->
    <update id="addAccountQuota" parameterType="com.lottery.infrastructure.persistent.po.RaffleActivityAccount">
        update raffle_activity_account
        <set>
            <if test="totalCount != null">
                total_count = total_count + #{totalCount},
            </if>
            <if test="totalCountSurplus != null">
                total_count_surplus = total_count_surplus + #{totalCountSurplus},
            </if>
            update_time = now()
        </set>
        where user_id = #{userId} and activity_id = #{activityId}
    </update>


    <!--int updateActivityAccountSubtractionQuota(RaffleActivityAccount raffleActivityAccount);-->
    <update id="updateActivityAccountSubtractionQuota">
        update raffle_activity_account
        set total_count_surplus = total_count_surplus - 1,
            update_time = now()
        where user_id = #{userId} and activity_id = #{activityId} and total_count_surplus >= 1
    </update>

    <!-- int updateActivityAccountSpecifiedSubtractionQuota(RaffleActivityAccount raffleActivityAccount,Integer subtractor);;-->
    <update id="updateActivityAccountSpecifiedSubtractionQuota">
        update raffle_activity_account
        set total_count_surplus = total_count_surplus - #{subtractor},
            update_time = now()
        where user_id = #{raffleActivityAccount.userId} and activity_id = #{raffleActivityAccount.activityId} and total_count_surplus >= #{subtractor}
    </update>


    <select id="queryRaffleActivityAccount" resultMap="dataMap">
        select *
        from raffle_activity_account
        <where>
            <if test="userId != null">
                user_id = #{userId}
            </if>
            <if test="activityId != null">
                and activity_id = #{activityId}
            </if>
        </where>
    </select>


</mapper>
